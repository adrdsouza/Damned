/**
 * Query keys for Medusa JS SDK yment imperations.
 */
export const queryKeys = {
  cart: () => ["cart"],
  // Add other keys as needed, e.g.,
  // region: (regionId: string) => ["region", regionId],
  // product: (productId: string) => ["product", productId],
}